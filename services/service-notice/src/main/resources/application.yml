spring:
  application:
    name: service-notice

  profiles:
    active: local

  main:
    web-application-type: servlet

management:
  endpoints:
    web:
      exposure:
        include: health,info
