spring:
  application:
    name: service-member

  profiles:
    active: local

  main:
    web-application-type: servlet

springdoc:
  swagger-ui:
    disable-swagger-default-url: true
    config-url: ${app.swagger.gateway-prefix}/v3/api-docs/swagger-config
    url: ${app.swagger.gateway-prefix}/v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8083

app:
  swagger:
    enabled: true
    gateway-prefix: /member
