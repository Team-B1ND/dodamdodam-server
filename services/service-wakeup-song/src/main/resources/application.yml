spring:
  application:
    name: service-wakeup-song

  profiles:
    active: local

  main:
    web-application-type: servlet

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8085
