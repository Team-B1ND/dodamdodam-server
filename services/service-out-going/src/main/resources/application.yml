spring:
  application:
    name: service-out-going

  profiles:
    active: local

  main:
    web-application-type: servlet

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8084
