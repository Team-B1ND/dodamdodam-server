spring:
  application:
    name: service-outsleeping

  profiles:
    active: local

  main:
    web-application-type: servlet

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8083
